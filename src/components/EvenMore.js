import React from 'react';
// import photos from './json/photos.json';

function EvenMore() {
 
    return (
      <div className="App">
        <h1>More JSONs</h1>
        <p>put the json files below</p>
        
        {/* <div>
            {photos.map((photo) => {
                return <p>{JSON.stringify(photo)}</p>
            })}
        </div> */}

      </div>
    );
  }
  
  export default EvenMore;
  